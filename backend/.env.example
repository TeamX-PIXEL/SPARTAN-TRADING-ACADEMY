# --- JWT Secrets ---
SECRET_KEY=
ALGORITHM="HS256"

# --- Raw MySQL Connection (For Signal Bot) ---
MYSQL_HOST="localhost"
MYSQL_USER="root"
MYSQL_PASSWORD=
MYSQL_DATABASE=
DB_TABLE_USERS="signal_users"

# --- Telegram Bot Tokens ---
EVERGREEN_BOT_TOKEN=
LEGACY_BOT_TOKEN=
ALPHA_BOT_TOKEN=

# --- SQLAlchemy DATABASE_URL ---
DATABASE_URL="mysql+mysqlconnector://root:PASSWORD@127.0.0.1:3306/DATABASE_NAME"

# ==========================================
# SMTP / EMAIL CONFIGURATION (Gmail)
# ==========================================
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME="Spartan Trading Academy"

# CORS - allowed origins (comma-separated)
CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# Base URL for verification links (use your ngrok or domain URL)
BASE_URL="http://localhost:8000"

# Manual Session ID (Bypasses 2FA)
TV_SESSIONID=

# Product Aliases (Format: PINE_Name=ID)
PINE_TestScript=
